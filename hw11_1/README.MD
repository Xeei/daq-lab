# Intruction
1. download application from gitlab that provided in lecture slide
2. dow load `openapi=generator-cli.jar` from swagger website (i already provided in repo u can download it)
3. generate flask server with the command that provided in lecture slide
4. then install the requirement lib
5. start the app **there will be some problem with app** just use ai to fix it because the problem is version diff waste of time to fix it by yourself
6. install `openapi-to-graphql-cli` with npm and it might require more from lecture slide
7. start the graphql app with the command that provided from lecture slide 
8. implent task from lecture slide
- new api in rain-api-with-links.yaml
- make link in basin refference

# Note
There might be problem when implement new api such as
- app.py need to change to target `rain-api-with-links.yaml`
- change from `operateId` to `operateRef` to prevent uniqueId problem

GL ;)